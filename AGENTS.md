# 今日清单项目约束

- 程序必须以普通用户权限运行，不得请求管理员权限。
- 程序运行时默认离线，不得添加网络、遥测、广告或自动更新功能。
- 未经用户单独批准，不得安装新的第三方依赖、修改注册表、创建计划任务或写入项目目录和应用专用数据目录以外的位置。
- 开机启动只能通过当前用户 Startup 文件夹中的、由本程序创建并验证目标的快捷方式实现。
- 桌面层功能只能使用有边界的 Windows Shell 互操作；不得结束、重启、注入或修改 Explorer 进程。
- 所有持久化写入必须先验证、再原子替换，并保留可恢复副本。
- 日志不得包含任务标题、备注或其他用户正文。
- 每项功能必须有对应测试或明确的手工验收步骤，发布前必须通过 Release 构建和全部测试。

